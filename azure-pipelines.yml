trigger:
- main 

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: Docker@2
  displayName: 'Docker image builder'
  inputs:
    command: build
    dockerfile: '$(Build.SourcesDirectory)/Dockerfile'
    tags: |
        latest 